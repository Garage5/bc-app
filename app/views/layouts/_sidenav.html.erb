<div class="pod tnav">
	<div class="hd">
		<h2>Your Tournaments</h2>
	</div><!-- / hd -->
	<div class="bd">
		<ul>
		  <% if !@instance.tournaments.blank? %>
		    <% for t in @instance.tournaments %>
		      <li><%= link_to t.name, tournament_path(t) %></li>
		    <% end %>
		  <% end %>
			<li class="last"><%= link_to "Create a New Tournament", new_tournament_path, :class => 'icon-add' %></li>
		</ul>
	</div><!-- / bd -->
</div><!-- / pod -->

<div class="pod tnav">
	<div class="hd">
		<h2>Available Tournaments</h2>
	</div><!-- / hd -->
	<div class="bd">
		<ul>
			<li><a href="">Toynamant</a></li>
		  <li class="last"><a href="">Toynamant</a></li>
		</ul>
	</div><!-- / bd -->
</div><!-- / pod -->